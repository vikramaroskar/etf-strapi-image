'use strict';

/**
 * authorized-participant service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::authorized-participant.authorized-participant');
